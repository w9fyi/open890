#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

if [[ "${1:-}" == "" ]]; then
  echo "Usage: ./pushrelease vX.Y.Z"
  echo "Example: ./pushrelease v0.1.4"
  exit 1
fi

exec "$SCRIPT_DIR/scripts/publish_release.sh" "$@"
